name: Deploy Web Dinamis to AWS EC2

on:
  push:
    branches: [ main ]
    paths:
      - 'web-dinamis/**'
      - '.github/workflows/deploy-web-dinamis.yml'
  workflow_dispatch:

jobs:
  build-and-deploy-web-dinamis:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./web-dinamis
          file: ./web-dinamis/Dockerfile
          push: true
          no-cache: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/web-dinamis_uas_036:latest

  deploy-to-ec2:
    runs-on: ubuntu-latest
    needs: build-and-deploy-web-dinamis
    steps:
      - name: Deploy Web Dinamis via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.AWS_HOST }}
          username: ${{ secrets.AWS_USERNAME }}
          key: ${{ secrets.AWS_PRIVATE_KEY }}
          port: 22
          script: |
            cd /home/ubuntu/UAS_Khosyyatillah/web-dinamis
            git fetch origin main
            git reset --hard origin/main
            sudo docker pull ochakhosyyatillah/web-dinamis_uas_036:latest
            sudo DOCKERHUB_USERNAME=ochakhosyyatillah docker compose up -d --force-recreate --no-deps container-dinamis
            sudo docker image prune -af
            echo "Resetting and importing database on EC2..."
            sudo docker compose exec -T db-webdinamis mysql -u root -p'password!123' -e "DROP DATABASE IF EXISTS db_uas; CREATE DATABASE db_uas;"
            sudo docker compose exec -T db-webdinamis mysql -u root -p'password!123' db_uas < db_data/db_uas.sql
            echo "=== Status ==="
            sudo docker ps